import React from 'react';
import { SafeAreaView, Text, View} from 'react-native';

const Favorites = (props) => {
    return (
        <SafeAreaView>
            <View>
                <Text>Favorites</Text>
            </View>
        </SafeAreaView>
    );
};

export { Favorites }
